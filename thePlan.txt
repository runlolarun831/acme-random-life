Basic Goals
When a button is clicked, add a randomly generated item to the corresponding category
   -make a button that creates an item on click
  -creates a list item w/ a detroy button
Allow the user to destroy each item

no routes. video example stays on same page
look at week 9 for examples
10fetchUsers-

DONE 1.notes first---need to fix destroy note
2.vacations
3.followings






              followingCompanies.map( (followed, idx)  => {
                return (
                  <div key={followed.companyId}>
                    <div>{followed.companyId}</div>
                    <div>hmmm?</div>
                    <button>Destroy</button>
                  </div>
                );
              })
